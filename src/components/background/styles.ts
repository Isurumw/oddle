import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    resizeMode: 'stretch',
    width: '100%',
    height: '100%',
    position: 'absolute',
  },
});

export default styles;
